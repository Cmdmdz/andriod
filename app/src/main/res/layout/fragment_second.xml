<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:id="@+id/layoutsV1"
        android:layout_width="412dp"
        android:layout_height="89dp"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="-2dp"
        tools:layout_editor_absoluteY="1dp"
        tools:ignore="MissingConstraints">

        <LinearLayout
            android:id="@+id/layoutsH1"
            android:layout_width="match_parent"
            android:layout_height="69dp"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnUpComing"
                style="?android:attr/buttonBarButtonStyle"
                android:layout_width="210dp"
                android:layout_height="68dp"
                android:background="#FFFFFF"
                android:backgroundTint="@android:color/white"
                android:text="@string/upcoming"
                android:textColor="@color/black"
                android:onClick="OnUpComing"
                android:textColorHighlight="@color/black" />

            <Button
                android:id="@+id/btnHistory"
                style="?android:attr/buttonBarButtonStyle"
                android:layout_width="211dp"
                android:layout_height="68dp"
                android:background="@color/white"
                android:text="@string/hitsotry"
                android:textColor="@color/black" />
        </LinearLayout>
    </LinearLayout>

    <Button
        android:id="@+id/btnMap"
        android:layout_width="213dp"
        android:layout_height="78dp"
        android:rotationX="-5"
        android:shadowRadius="15"
        android:text="@string/btnMake"
        android:onClick="OnToLocation"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layoutsV1"
        app:layout_constraintVertical_bias="0.822" />

</androidx.constraintlayout.widget.ConstraintLayout>